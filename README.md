# Task Tracker API

## Project Description

Task Tracker is a Module 1 learning project built with Python and FastAPI.

The current implementation provides the foundation for a REST API backend, including:

- FastAPI application setup
- Pydantic validation support
- Health check endpoint

Future modules may add task CRUD operations, filtering, and local JSON storage.

## Project Structure

